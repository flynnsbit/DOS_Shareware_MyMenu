1. Абсолютный путь и показ файлов при отсутствии опций
   -q (quiet - only errors), -b (file names only, errors to stderr),
   default files with attributes
2. Сравнение/копирование даты/времени/атрибутов файла с файлом
3. Увеличение/уменьшение даты и времени: /d[<d>][+-<d>], /t[<t>][+-<t>]
4. сравнение атрибутов на больше/меньше?
5. Документация: "помимо этого STAMP позволяет генерить списки файлов"
6. "n ame1"\nam e2"\name3
7. проверить UNC/NetWare
